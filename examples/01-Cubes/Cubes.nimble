# Package
version     = "0.1.0"
author      = "Cory Noll Crimmins - Golden"
description = "Cubes example for nim-bgfx"
license     = "BSD"
bin         = @["Cubes"]
backend     = "cpp"

# Dependencies
requires "bgfx >= 0.1.0"
requires "nim >= 0.14.2"
requires "glfw >= 3.2.0"
requires "FPMath >= 0.1.0"
